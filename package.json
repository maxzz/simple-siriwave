{
    "name": "simple-siriwave",
    "description": "This is a refactoring and embedding practice.",
    "version": "0.0.0",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "predeploy": "rm -rf dist && yarn build",
        "deploy": "gh-pages -d dist"
    },
    "dependencies": {
        "vue": "^3.0.4"
    },
    "devDependencies": {
        "@vue/compiler-sfc": "^3.0.4",
        "sass": "^1.30.0",
        "vite": "^1.0.0-rc.13"
    }
}
